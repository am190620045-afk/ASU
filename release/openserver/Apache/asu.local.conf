# ASU Open Server Apache snippet
# Included by Open Server project configuration

<Directory "{base_dir}/public">
    AllowOverride All
    Require all granted
</Directory>

<FilesMatch "^\.env">
    Require all denied
</FilesMatch>

<FilesMatch "(composer\.(json|lock)|package\.json)$">
    Require all denied
</FilesMatch>

# Step 02 - Composer Setup

Requirements:

- PHP 8.5 recommended
- Composer installed

Run:

```bash
composer install --no-dev --optimize-autoloader
```

Verify:

```bash
composer validate
```

Ensure writable directories are configured according to ASU runtime requirements.

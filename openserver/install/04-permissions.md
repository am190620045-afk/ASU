# Step 04 - Runtime Permissions

Required writable paths depend on ASU framework/runtime configuration.

Check:

- application storage directories
- cache directories
- log directories
- upload directories

Apache user must have required write access.

Do not make the whole project writable.
